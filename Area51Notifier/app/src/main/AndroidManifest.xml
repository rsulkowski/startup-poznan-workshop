<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.tomtom.example.areanotifier">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ic_launcher"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".SplashActivity"
                  android:configChanges="orientation|screenSize"
                  android:label="@string/app_name"
                  android:noHistory="true"
                  android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <!-- TODO: Add proper api keys -->
        <!--<meta-data-->
                <!--android:name="OnlineMaps.Key"-->
                <!--android:value=""/>-->
        <!--<meta-data-->
                <!--android:name="OnlineRouting.Key"-->
                <!--android:value=""/>-->
        <!--<meta-data-->
                <!--android:name="OnlineSearch.Key"-->
                <!--android:value=""/>-->
        <!--<meta-data-->
                <!--android:name="OnlineTraffic.Key"-->
                <!--android:value=""/>-->
        <!--<meta-data-->
                <!--android:name="GeofencingApi.Key"-->
                <!--android:value=""/>-->

    </application>

</manifest>